# ENV VARS
AWS_PROFIL= < profil name > 
AWS_REGION=us-east-1
ANSIBLE_SSH_PRIVATE_KEY_FILE_PATH= < path to .pem file >
# TERRAFORM SPECIFIC ENV VARS
TF_VAR_ssh_private_key_file_path=${ANSIBLE_SSH_PRIVATE_KEY_FILE_PATH}
